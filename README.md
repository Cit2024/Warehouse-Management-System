# 📦 منظومة إدارة المخازن (Warehouse Management System)

تطبيق سطح مكتب (Desktop Application) تم تطويره لصالح **كلية التقنية الصناعية – مصراتة** لإدارة المخازن، الأذونات، ومتابعة الأرصدة. يعمل النظام بشكل محلي بالكامل بدون خوادم خارجية، مع التركيز على السرعة، استقرار البيانات، والأمان.

**الإصدار الحالي:** `v1.0.4`  
**الترخيص:** MIT

---

## 🎯 نظرة عامة على سير العمل

المستخدم يبدأ بتسجيل الدخول، ثم ينتقل إلى لوحة التحكم التي تخبره مباشرةً بحالة المخزن. من هناك يمكنه إجراء توريد أو صرف، مراجعة التقارير، أو إدارة الأصناف والجهات.

```mermaid
flowchart TD
    A[شاشة تسجيل الدخول] --> B{تحديد الدور}
    B -->|مسؤول Admin| C[إدارة المستخدمين<br/>والنسخ الاحتياطي]
    B -->|أمين مخزن| D[لوحة التحكم]
    B -->|مستعرض| D
    D --> E[توريد جديد]
    D --> F[صرف جديد]
    D --> G[التقارير والطباعة]
    D --> H[الأصناف والمخزون]
    D --> I[الموردون والجهات]
    D --> J[الإعدادات والاسترجاع]
    E --> K[(قاعدة بيانات SQLite)]
    F --> K
    G --> K
    H --> K
    I --> K
    C --> K
    J --> K
```

---

## 👥 الأدوار وصلاحيات المستخدمين

| الدور | الوصف | الصلاحيات الرئيسية |
| :--- | :--- | :--- |
| **مسؤول (Admin)** | مدير النظام | كل الصلاحيات: إضافة/تعديل/حذف الأصناف والجهات، إصدار أذونات التوريد والصرف، إدارة المستخدمين (إضافة، تغيير الأدوار، تفعيل/تعطيل، إعادة تعيين كلمات المرور)، إعداد التقارير، النسخ الاحتياطي والاسترجاع. |
| **أمين مخزن (Store_Keeper)** | المستخدم اليومي للمخزن | إضافة/تعديل/حذف الأصناف والجهات، إصدار أذونات التوريد والصرف، إعداد التقارير، عرض النسخ الاحتياطية والاسترجاع. **لا يستطيع** إدارة المستخدمين. |
| **مستعرض (Viewer)** | مراقب / قارئ فقط | عرض لوحة التحكم، الأصناف، الجهات، التقارير، وسجل الحركات. **لا يستطيع** إضافة أو تعديل أو حذف أي بيانات أو إصدار أذونات. |

> **ملاحظة أمان:** كل فحوصات الصلاحيات تُنفّذ في العملية الرئيسية (main process) على الخادم المحلي، وليس في الواجهة فقط.

---

## ✨ قائمة الميزات والاستخدام

> الصور أدناه لقطات حقيقية من واجهة التطبيق أثناء التشغيل (بيانات تجريبية للعرض) وتم حفظها في مجلد `placeholders/`.

### 1. تسجيل الدخول والأمان
- شاشة دخول بسيطة تطلب الاسم وكلمة المرور.
- كلمات المرور مُخزّنة باستخدام **scrypt** (وليس نصاً واضحاً).
- دعم تغيير كلمة المرور الشخصية.
- المسؤول يستطيع إعادة تعيين كلمة مرور أي مستخدم آخر.

<p align="center"><img src="placeholders/login.png" width="500" alt="شاشة تسجيل الدخول"></p>

### 2. لوحة التحكم
- **شريط حالة كبير** يخبر المستخدم بجملة عربية واضحة: "كل شيء على ما يرام" أو "يوجد N صنف تحتاج إعادة طلب".
- عند النقر على شريط التحذير، يتم الانتقال تلقائياً إلى قائمة الأصناف المنخفضة.
- **ثلاثة أزرار رئيسية كبيرة**: توريد جديد، صرف جديد، طباعة تقرير.
- **قسم يحتاج اهتمامك**: يعرض الأصناف التي وصلت للحد الأدنى مع جملة "متبقي X من أصل حد أدنى Y".
- **آخر الحركات**: آخر 5 حركات مكتوبة كجمل عربية (مثال: "أمس — تم صرف 20 قلم حبر لقسم الشؤون الإدارية").
- **ملخص أرقام صغير** في الأسفل: إجمالي الأصناف، قيمة المخزون، حركات الشهر، مع تلميحات توضح معنى كل رقم.

<p align="center"><img src="placeholders/dashboard.png" width="700" alt="لوحة التحكم"></p>

### 3. الأصناف والمخزون
- إضافة صنف جديد: الاسم، الوحدة، التصنيف، الحد الأدنى، السعر.
- البحث والتصفية حسب الاسم/الرقم/التصنيف.
- تعديل بيانات الصنف.
- حذف منطقي (Soft Delete) لضمان عدم فقدان البيانات المحاسبية.

<p align="center"><img src="placeholders/items.png" width="700" alt="الأصناف والمخزون"></p>

### 4. الموردون والجهات
- تسجيل الموردين الخارجيين والأقسام/الموظفين الداخليين.
- البحث بالاسم أو رقم الهاتف.
- تعديل وحذف منطقي للجهات.

<p align="center"><img src="placeholders/entities.png" width="700" alt="الموردون والجهات"></p>

### 5. أذونات التوريد
- اختيار المورد، المخزن، وتاريخ التوريد.
- إضافة عدة أصناف بكميات وأسعار.
- حفظ الإذن والطباعة مباشرة.

| نموذج إذن التوريد | إذن التوريد المطبوع |
| :---: | :---: |
| <img src="placeholders/supply.png" width="400" alt="نموذج إذن التوريد"> | <img src="placeholders/reports/report-supply-receipt.png" width="400" alt="إذن توريد مطبوع"> |

### 6. أذونات الصرف
- اختيار الجهة الطالبة والمخزن.
- **التحقق اللحظي من الرصيد**: يمنع صرف كمية أكبر من المتاح.
- حفظ الإذن والطباعة مباشرة.

| نموذج إذن الصرف | إذن الصرف المطبوع |
| :---: | :---: |
| <img src="placeholders/dispense.png" width="400" alt="نموذج إذن الصرف"> | <img src="placeholders/reports/report-dispense-receipt.png" width="400" alt="إذن صرف مطبوع"> |

### 7. التقارير والطباعة
- تسعة أنواع تقارير:
  1. تقرير حالة المخزون
  2. دليل الأصناف الأساسي
  3. تقرير الأصناف منخفضة الرصيد
  4. تقرير الجرد الفعلي
  5. سجل حركات التوريد والصرف
  6. إذن توريد
  7. إذن صرف
  8. بطاقة حركة صنف
  9. دليل الموردين والجهات
- طباعة منبثقة بقياس A4 مع توقيعات رسمية.
- تصدير/طباعة PDF بدعم اللغة العربية.
- اختيار الأعمدة الظاهرة لكل تقرير.

<p align="center"><img src="placeholders/report.png" width="700" alt="صفحة التقارير"></p>

| تقرير حالة المخزون | دليل الأصناف الأساسي |
| :---: | :---: |
| <img src="placeholders/reports/report-stock.png" width="400" alt="تقرير حالة المخزون"> | <img src="placeholders/reports/report-items-base.png" width="400" alt="دليل الأصناف الأساسي"> |

| تقرير الأصناف منخفضة الرصيد | تقرير الجرد الفعلي |
| :---: | :---: |
| <img src="placeholders/reports/report-lowstock.png" width="400" alt="تقرير الأصناف منخفضة الرصيد"> | <img src="placeholders/reports/report-inventory-count.png" width="400" alt="تقرير الجرد الفعلي"> |

| سجل الحركات | بطاقة حركة صنف |
| :---: | :---: |
| <img src="placeholders/reports/report-movements.png" width="400" alt="سجل الحركات"> | <img src="placeholders/reports/report-item-card.png" width="400" alt="بطاقة حركة صنف"> |

| دليل الموردين والجهات |
| :---: |
| <img src="placeholders/reports/report-suppliers.png" width="400" alt="دليل الموردين والجهات"> |

### 8. إدارة المستخدمين (للمسؤول فقط)
- إضافة مستخدم جديد.
- تغيير دور المستخدم.
- تفعيل/تعطيل الحساب.
- إعادة تعيين كلمة المرور.

<p align="center"><img src="placeholders/users.png" width="700" alt="إدارة المستخدمين"></p>

### 9. النسخ الاحتياطي والاسترجاع
- نسخ احتياطي محلي تلقائي على نفس الجهاز.
- استرجاع أي نسخة سابقة من سجل Git المحلي.
- المزامنة السحابية معطّلة في هذا الإصدار.

<p align="center"><img src="placeholders/settings.png" width="700" alt="الإعدادات والنسخ الاحتياطي"></p>

---

## 🛠️ التقنيات المستخدمة (Tech Stack)

| الطبقة | التقنية |
| :--- | :--- |
| الإطار الرئيسي | [Electron.js](https://www.electronjs.org/) |
| قاعدة البيانات | [SQLite](https://www.sqlite.org/) عبر `better-sqlite3` |
| الواجهات | HTML5, CSS3, JavaScript (Vanilla) — لا يوجد إطار frontend |
| الأمان | CSP headers، هروب النصوص (escape)، فحوصات الصلاحيات في main process |
| البناء | `electron-builder` (Windows `.exe`، Linux `.AppImage`/`.deb`) |

---

## 🚀 كيفية التشغيل في بيئة التطوير

**1. تحميل المستودع:**

```bash
git clone https://github.com/RNotmyname/cit.git
cd cit
```

**2. تثبيت المكتبات:**

```bash
npm install
```

**3. تشغيل التطبيق:**

```bash
npm start
```

---

## 📦 بناء الإصدار للإنتاج

**ويندوز (ملف `.exe`):**

```bash
npm run build:win
```

**لينكس (ملف `.AppImage`):**

```bash
npm run build:linux
```

**لينكس (حزمة `.deb`):**

```bash
npm run build:linux-deb
```

---

## 🔐 ملاحظات أمنية

- النظام يعمل محلياً بالكامل؛ لا توجد اتصالات خارجية.
- صلاحيات الكتابة تُمنح فقط لـ **المسؤول** و**أمين المخزن**.
- **المستعرض (Viewer)** لا يستطيع تعديل أي بيانات حتى لو وصل إلى صفحة التعديل.
- يتم منع صرف أي كمية تتجاوز الرصيد المتاح قبل حفظ الإذن.

---

## 📄 الملفات الرئيسية

| الملف | الغرض |
| :--- | :--- |
| `main.js` | العملية الرئيسية لـ Electron، معالجات IPC، قواعد البيانات |
| `preload.js` | جسر IPC الآمن بين الواجهة والعملية الرئيسية |
| `database/db.js` | إعداد قاعدة البيانات والاتصال |
| `assets/js/pages/*.js` | منطق كل صفحة |
| `assets/css/*.css` | الأنماط المشتركة بين الصفحات |
| `release/` | مخرجات البناء الجاهزة للتوزيع |

---

> **الجهة المنفذة:** قسم تقنية المعلومات — كلية التقنية الصناعية.
